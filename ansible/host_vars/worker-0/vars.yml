---
ansible_become_password: "{{ vault_worker_0_become_password }}"
hostname: worker-0

# MAC-Adresse des NIC von worker-0, fuer Wake-on-LAN
# (ansible/roles/wake_on_lan, ansible/roles/cluster_power_manager).
# CHANGE: auf worker-0 selbst ermitteln, z.B.:
#   ip link show | grep -A1 "^[0-9]*: en" | grep link/ether
wake_on_lan_mac_address: "98:fa:9b:28:b0:22"
