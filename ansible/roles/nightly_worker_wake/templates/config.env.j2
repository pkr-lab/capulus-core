# Managed by Ansible (nightly_worker_wake role) — do not edit manually.
MAX_RUNTIME_SECONDS={{ nightly_worker_wake_max_runtime_seconds }}
READY_TIMEOUT_SECONDS={{ nightly_worker_wake_ready_timeout_seconds }}
STOP_GRACE_SECONDS={{ nightly_worker_wake_stop_grace_seconds }}
POLL_SECONDS={{ nightly_worker_wake_poll_seconds }}
SSH_USER={{ nightly_worker_wake_ssh_user }}
SSH_KEY={{ nightly_worker_wake_ssh_key_path }}
KUBECONFIG_PATH={{ nightly_worker_wake_kubeconfig }}
STATE_DIR={{ nightly_worker_wake_state_dir }}
SEMAPHORE_API_BASE={{ nightly_worker_wake_semaphore_api_base }}
SEMAPHORE_ADMIN_USERNAME={{ nightly_worker_wake_semaphore_admin_username }}
SEMAPHORE_ADMIN_PASSWORD_PATH={{ nightly_worker_wake_semaphore_admin_password_path }}
NTFY_HOST={{ nightly_worker_wake_ntfy_host }}
NTFY_IP={{ nightly_worker_wake_ntfy_ip }}
NTFY_TOPIC={{ nightly_worker_wake_ntfy_topic }}
N8N_WEBHOOK_URL={{ nightly_worker_wake_n8n_webhook_url }}
WORKERS="{% for w in nightly_worker_wake_workers %}{{ w.name }}:{{ w.ansible_host }}:{{ w.mac_address }}{% if not loop.last %} {% endif %}{% endfor %}"
