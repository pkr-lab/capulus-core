# Managed by Ansible (power_agent role) — do not edit manually.
TOKEN={{ power_agent_token }}
PORT={{ power_agent_port }}
BACKLIGHT_PATH={{ power_agent_backlight_path }}
BACKLIGHT_MAX_PATH={{ power_agent_backlight_max_path }}
BL_POWER_PATH={{ power_agent_bl_power_path }}
SSH_USER={{ power_agent_ssh_user }}
SSH_KEY={{ power_agent_ssh_key }}
KUBECONFIG_PATH={{ power_agent_kubeconfig }}
STATE_DIR={{ power_agent_state_dir }}
POWEROFF_BIN={{ power_agent_poweroff_bin }}
TARGETS="{% for w in power_agent_targets %}{{ w.name }}:{{ w.ansible_host }}:{{ w.mac_address }}{% if not loop.last %} {% endif %}{% endfor %}"
