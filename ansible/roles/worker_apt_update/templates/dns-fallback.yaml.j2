# Managed by Ansible (worker_apt_update role) — do not edit manually.
#
# Ergaenzt nur "nameservers" fuer {{ worker_apt_update_dns_fallback_interface }} --
# Netplan merged das mit der bestehenden Subiquity-Konfiguration
# (Adresse/Route bleiben dort unveraendert), siehe
# docs/2-betrieb-hardware/20030-nightly-worker-update.md.
network:
  version: 2
  ethernets:
    {{ worker_apt_update_dns_fallback_interface }}:
      nameservers:
        addresses:
{% for dns in worker_apt_update_dns_fallback_servers %}
          - {{ dns }}
{% endfor %}
