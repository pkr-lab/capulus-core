apiVersion: v1
kind: Service
metadata:
  name: {{ include "day-pilot.fullname" . }}-postgres
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "day-pilot.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "day-pilot.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
