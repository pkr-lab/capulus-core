apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gotify-bridge.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gotify-bridge.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "gotify-bridge.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/script: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "gotify-bridge.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "gotify-bridge.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: bridge
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["python3", "/app/bridge.py"]
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: GOTIFY_URL
              value: {{ .Values.gotify.url | quote }}
            - name: GOTIFY_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.gotify.tokenSecretName }}
                  key: {{ .Values.gotify.tokenSecretKey }}
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          volumeMounts:
            - name: script
              mountPath: /app
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: script
          configMap:
            name: {{ include "gotify-bridge.fullname" . }}-script
            defaultMode: 0555
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
