apiVersion: v1
kind: Service
metadata:
  name: {{ include "ntfy-bridge.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ntfy-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ntfy-bridge.selectorLabels" . | nindent 4 }}
