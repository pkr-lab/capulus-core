apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "semaphore.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "semaphore.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "semaphore.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "semaphore.selectorLabels" . | nindent 8 }}
    spec:
      # Kubernetes injects SEMAPHORE_PORT=tcp://... for the in-namespace
      # Service, which Semaphore mistakes for its own Port config and panics.
      enableServiceLinks: false
      # The Semaphore image creates /etc/semaphore as UID 1001. Without
      # fsGroup the persistent volume mounts as root-only and the pod
      # fails to write its BoltDB. Matches the official upstream chart.
      securityContext:
        fsGroup: 1001
        runAsUser: 1001
        runAsGroup: 1001
      initContainers:
        - name: bootstrap-admin
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # SEMAPHORE_ADMIN* env vars are not auto-applied by the server in 2.18.x.
          # Running user add here (DB unlocked, before server starts) is idempotent.
          command:
            - sh
            - -c
            - |
              semaphore user add \
                --admin \
                --login    "$SEMAPHORE_ADMIN" \
                --name     "$SEMAPHORE_ADMIN_NAME" \
                --email    "$SEMAPHORE_ADMIN_EMAIL" \
                --password "$SEMAPHORE_ADMIN_PASSWORD" \
                || true
          env:
            - name: SEMAPHORE_DB_DIALECT
              value: "sqlite"
            - name: SEMAPHORE_DB_HOST
              value: "/etc/semaphore/database.db"
            - name: SEMAPHORE_ADMIN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrapSecretName }}
                  key: admin_username
            - name: SEMAPHORE_ADMIN_NAME
              value: "Home Server Admin"
            - name: SEMAPHORE_ADMIN_EMAIL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrapSecretName }}
                  key: admin_email
            - name: SEMAPHORE_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrapSecretName }}
                  key: admin_password
          volumeMounts:
            - name: data
              mountPath: /etc/semaphore
      containers:
        - name: semaphore
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          env:
            - name: SEMAPHORE_DB_DIALECT
              value: "sqlite"
            - name: SEMAPHORE_DB_HOST
              value: "/etc/semaphore/database.db"
            # Migrate existing BoltDB data to SQLite on first start (runs once,
            # only when database.db does not yet exist). Safe to keep afterwards.
            - name: SEMAPHORE_MIGRATE_FROM_BOLTDB
              value: "/etc/semaphore/database.boltdb"
            - name: SEMAPHORE_TMP_PATH
              value: "/tmp/semaphore"

            # Admin credentials — used by the bootstrap initContainer only.
            - name: SEMAPHORE_ADMIN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrapSecretName }}
                  key: admin_username
            - name: SEMAPHORE_ADMIN_NAME
              value: "Home Server Admin"
            - name: SEMAPHORE_ADMIN_EMAIL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrapSecretName }}
                  key: admin_email
            - name: SEMAPHORE_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrapSecretName }}
                  key: admin_password

            # 32-byte base64 key used to encrypt secrets inside Semaphore's DB.
            # CHANGING this after first run will invalidate all stored secrets.
            - name: SEMAPHORE_ACCESS_KEY_ENCRYPTION
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrapSecretName }}
                  key: access_key_encryption

            # Hand ansible-playbook the vault password from the mounted file.
            - name: ANSIBLE_VAULT_PASSWORD_FILE
              value: {{ printf "%s/ansible_vault_password" .Values.vaultPasswordMountDir | quote }}

            - name: SEMAPHORE_LDAP_ACTIVATED
              value: "no"

            # Cron schedules are evaluated in this timezone (Semaphore default
            # is UTC). Setting the home timezone makes a "0 6 * * *" schedule
            # fire at 06:00 local time year-round, instead of drifting one hour
            # between CET and CEST.
            - name: SEMAPHORE_SCHEDULE_TIMEZONE
              value: {{ .Values.scheduleTimezone | default "Europe/Berlin" | quote }}

          volumeMounts:
            - name: data
              mountPath: /etc/semaphore
            - name: tmp
              mountPath: /tmp/semaphore
            # Mount as a directory (no subPath) so fsGroup=1001 is applied
            # by the kubelet and the projected file is readable by the pod
            # (root:1001 0440). With subPath, fsGroup is skipped and the
            # file ends up root:root 0440 — unreadable by UID 1001.
            - name: vault-password
              mountPath: {{ .Values.vaultPasswordMountDir | quote }}
              readOnly: true

          # Use "/" like the official semaphoreui chart — /api/ping is not
          # guaranteed to exist across versions, the web root always returns 200.
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "semaphore.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: tmp
          emptyDir: {}
        - name: vault-password
          secret:
            secretName: {{ .Values.bootstrapSecretName }}
            # 0440 + fsGroup=1001 = file owned root:1001 mode 0440,
            # readable by the pod's GID 1001. fsGroup only applies because
            # the mount above is a directory mount (no subPath).
            defaultMode: 0440
            items:
              - key: ansible_vault_password
                path: ansible_vault_password
