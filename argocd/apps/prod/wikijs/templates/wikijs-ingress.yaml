{{- if .Values.wikijs.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "wikijs.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "wikijs.labels" . | nindent 4 }}
  {{- with .Values.wikijs.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.wikijs.ingress.className }}
  ingressClassName: {{ .Values.wikijs.ingress.className }}
  {{- end }}
  {{- if .Values.wikijs.ingress.tls }}
  tls:
    {{- range .Values.wikijs.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.wikijs.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "wikijs.fullname" $ }}
                port:
                  number: {{ $.Values.wikijs.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
