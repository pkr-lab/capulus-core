apiVersion: v1
kind: Service
metadata:
  name: {{ include "wikijs.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "wikijs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.wikijs.service.type }}
  ports:
    - port: {{ .Values.wikijs.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wikijs.selectorLabels" . | nindent 4 }}
