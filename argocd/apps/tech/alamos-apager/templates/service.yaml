apiVersion: v1
kind: Service
metadata:
  name: {{ include "alamos-apager.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "alamos-apager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alamos-apager.selectorLabels" . | nindent 4 }}
