apiVersion: v1
kind: Service
metadata:
  name: {{ include "gotify-bridge.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gotify-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gotify-bridge.selectorLabels" . | nindent 4 }}
