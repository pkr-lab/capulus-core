---
apiVersion: v2
name: ntfy-bridge
description: >
  Webhook bridge: translates Alertmanager JSON payloads into ntfy API calls
  with proper title, priority, and tag headers. Runs as a lightweight Python
  stdlib HTTP server — no external dependencies.
type: application
version: 0.1.0
appVersion: "1.0.0"
keywords:
  - alertmanager
  - ntfy
  - bridge
  - notifications
maintainers:
  - name: home-server-admin
