---
# HTTP-Server: Stellt autounattend.xml, PowerShell-Skripte und WinPE-Dateien bereit.
# Läuft auf worker-0 (NodeAffinity), weil der PVC "hdd" nur dort verfügbar ist.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: windeployment-http
  namespace: windeployment
  labels:
    app: windeployment-http
spec:
  replicas: 0
  selector:
    matchLabels:
      app: windeployment-http
  template:
    metadata:
      labels:
        app: windeployment-http
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - worker-0
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
            - name: autounattend
              mountPath: /usr/share/nginx/html/autounattend.xml
              subPath: autounattend.xml
            - name: autounattend
              mountPath: /usr/share/nginx/html/autounattend-usb.xml
              subPath: autounattend-usb.xml
            - name: scripts
              mountPath: /usr/share/nginx/html/scripts
            - name: ipxe-menu
              mountPath: /usr/share/nginx/html/ipxe
            - name: winpe-files
              mountPath: /mnt/winpe
              subPath: winpe
            - name: winpe-files
              mountPath: /mnt/iso
              subPath: iso
            - name: pccfg
              mountPath: /usr/share/nginx/html/pc.cfg
              subPath: pc.cfg
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 30
        # Zähler-Sidecar: vergibt fortlaufende PC-Nummern (1002011-XXXX).
        # Läuft nur auf localhost, nginx reicht /api/next-number durch.
        - name: counter
          image: python:3.12-alpine
          command: ["python3", "/app/counter.py"]
          env:
            - name: MINIO_ENDPOINT
              value: "minio.minio.svc.cluster.local:9000"
            - name: MINIO_BUCKET
              value: "windows-pc-logs"
            - name: ZAMMAD_URL
              value: "http://zammad.homeserver"
            - name: ZAMMAD_GROUP
              value: "Windows-PCs"
            - name: ZAMMAD_CUSTOMER
              value: "windeployment@dlrg.local"
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: windeployment-credentials
                  key: minioAccessKey
                  optional: true
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: windeployment-credentials
                  key: minioSecretKey
                  optional: true
            - name: ZAMMAD_TOKEN
              valueFrom:
                secretKeyRef:
                  name: windeployment-credentials
                  key: zammadToken
                  optional: true
          volumeMounts:
            - name: counter-script
              mountPath: /app
            - name: winpe-files
              mountPath: /data
              subPath: counter
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "100m"
      volumes:
        - name: nginx-config
          configMap:
            name: nginx-config
        - name: autounattend
          configMap:
            name: autounattend-configs
        - name: scripts
          configMap:
            name: setup-scripts
        - name: ipxe-menu
          configMap:
            name: ipxe-menu
            items:
              - key: menu.ipxe
                path: menu.ipxe
        - name: pccfg
          configMap:
            name: pc-cfg
        - name: counter-script
          configMap:
            name: pc-counter-script
        - name: winpe-files
          persistentVolumeClaim:
            claimName: windeployment-files
---
# PXE-TFTP-Server: Liefert iPXE-Bootloader an bootende PCs.
# Benötigt hostNetwork für UDP-Port 69 (TFTP).
# Läuft auf worker-0 (NodeAffinity + hostNetwork → derselbe Node wie der HTTP-Server).
# Optional - nur benötigt wenn PXE-Boot ohne USB-Stick verwendet werden soll.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: windeployment-pxe
  namespace: windeployment
  labels:
    app: windeployment-pxe
spec:
  replicas: 0
  selector:
    matchLabels:
      app: windeployment-pxe
  template:
    metadata:
      labels:
        app: windeployment-pxe
    spec:
      # hostNetwork: TFTP läuft auf Port 69 (< 1024), kein NodePort möglich.
      # NodeAffinity: muss auf worker-0 laufen (PVC + TFTP im LAN).
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - worker-0
      initContainers:
        # Init-Container: iPXE-Binaries aus dem offiziellen Release herunterladen.
        # Legt Dateien in /tftpboot (PVC-Subpath) ab; beim Neustart ueberspringen.
        - name: download-ipxe
          image: alpine:3.20
          command:
            - sh
            - -c
            - |
              set -e
              cd /tftpboot
              if [ ! -f undionly.kpxe ]; then
                echo "Lade iPXE-Binaerdateien..."
                wget -q "https://boot.ipxe.org/undionly.kpxe" -O undionly.kpxe
                wget -q "https://boot.ipxe.org/ipxe.efi" -O ipxe.efi
                echo "iPXE geladen: $(ls -lh)"
              else
                echo "iPXE bereits vorhanden: $(ls -lh)"
              fi
          volumeMounts:
            - name: tftpboot
              mountPath: /tftpboot
      containers:
        - name: dnsmasq-pxe
          image: 4km3/dnsmasq:2.86
          args:
            - --conf-file=/etc/dnsmasq-pxe/dnsmasq-pxe.conf
            - --no-daemon
            - --log-facility=-
          ports:
            - containerPort: 69
              protocol: UDP
              hostPort: 69
            - containerPort: 67
              protocol: UDP
              hostPort: 67
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
                - NET_RAW
          volumeMounts:
            - name: dnsmasq-config
              mountPath: /etc/dnsmasq-pxe
            - name: tftpboot
              mountPath: /tftpboot
          resources:
            requests:
              memory: "32Mi"
              cpu: "20m"
            limits:
              memory: "64Mi"
              cpu: "100m"
      volumes:
        - name: dnsmasq-config
          configMap:
            name: ipxe-menu
            items:
              - key: dnsmasq-pxe.conf
                path: dnsmasq-pxe.conf
        - name: tftpboot
          persistentVolumeClaim:
            claimName: windeployment-files
            readOnly: false
