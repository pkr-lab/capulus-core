apiVersion: v1
kind: Service
metadata:
  name: {{ include "alamos-relay.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "alamos-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alamos-relay.selectorLabels" . | nindent 4 }}
