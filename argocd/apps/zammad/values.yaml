---
# ============================================================
# Zammad — Wrapper-Chart values
#
# Secrets-Block (SealedSecret):
#   Werte mit kubeseal versiegeln und hier eintragen.
#   Anleitung: docs/17-zammad.md → Abschnitt "Schritt 1 — Secrets versiegeln"
# ============================================================

secrets:
  enabled: true
  name: zammad-secrets
  # Versiegelte Ciphertext-Werte — NIEMALS Klartext committen!
  # Versiegeln: echo -n 'WERT' | kubeseal --raw --namespace zammad \
  #             --name zammad-secrets --controller-namespace sealed-secrets \
  #             --controller-name sealed-secrets-controller
  #
  # Beide Keys müssen dasselbe Passwort enthalten:
  #   postgresql-pass      → Key für den Zammad-App-Container
  #   postgresql-password  → Key für den Bitnami-PostgreSQL-Sub-Chart
  encryptedPostgresPass: ""
  encryptedPostgresPassword: ""
  # redis-password → Key für Zammad-App-Container + CloudPirates-Redis-Sub-Chart (gemeinsames Passwort)
  encryptedRedisPassword: ""

# ============================================================
# Sub-Chart: zammad/zammad v16.4.1
# Alle Keys unterhalb von `zammad:` landen beim upstream-Chart.
# Upstream values.yaml:
#   https://github.com/zammad/zammad-helm/blob/main/zammad/values.yaml
# ============================================================

zammad:
  # ---- Ingress -----------------------------------------------
  ingress:
    enabled: true
    className: traefik
    replicas: 1
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    hosts:
      - host: zammad.homeserver
        paths:
          - path: /
            pathType: Prefix
    tls: []

  # ---- Secrets (aus bestehendem SealedSecret) ----------------
  # Zammad liest das DB-Passwort aus `zammad-secrets[postgresql-pass]`.
  secrets:
    postgresql:
      useExisting: true
      secretKey: postgresql-pass
      secretName: zammad-secrets
    # Redis-Passwort aus existierendem SealedSecret (zammad-secrets[redis-password]).
    # Hinweis: Passwortloses Redis ist mit diesem Chart nicht nutzbar — REDIS_URL
    # wird immer als "redis://:$(REDIS_PASSWORD)@..." gebaut; ohne definierte
    # REDIS_PASSWORD-Var landet der wörtliche String "$(REDIS_PASSWORD)" im
    # Passwort-Feld und Zammad sendet ein AUTH-Kommando an ein Redis ohne Auth.
    redis:
      useExisting: true
      secretKey: redis-password
      secretName: zammad-secrets

  # ---- Zammad App + Sub-Chart Konfiguration ------------------
  zammadConfig:
    # ---- Elasticsearch deaktivieren (Ressourcenersparnis) ----
    # Zammad 7.x nutzt PostgreSQL-basierte Volltextsuche als Fallback.
    # Für Produktionseinsatz mit großem Ticketvolumen ES aktivieren.
    elasticsearch:
      enabled: false
      # initialisation ist standardmäßig true, unabhängig von enabled — ohne
      # diese Zeile versucht der zammad-init-Job trotzdem, ein (nicht
      # vorhandenes) Elasticsearch zu initialisieren, und der Job schlägt fehl.
      initialisation: false

    # ---- Memcached (aktiv — Standard-Cache) -----------------
    memcached:
      enabled: true

    # ---- PostgreSQL (intern, Bitnami Sub-Chart) -------------
    postgresql:
      enabled: true
      db: zammad_production
      user: zammad
      # pass leer lassen → kommt aus secrets.postgresql (useExisting: true)
      pass: ""

    # ---- Redis (intern, CloudPirates OCI Sub-Chart) ---------
    redis:
      enabled: true
      # pass leer lassen → kommt aus secrets.redis (useExisting: true)
      pass: ""

    # ---- Datei-Ablage (deaktiviert → Dateien in DB) ---------
    # Attachments werden in PostgreSQL gespeichert.
    # Für separates PVC: storageVolume.enabled=true +
    #   existingClaim (ReadWriteMany) — auf single-node k3s nicht Standard.
    storageVolume:
      enabled: false

    # ---- Nginx (Web-Frontend) --------------------------------
    nginx:
      resources:
        requests:
          cpu: 25m
          memory: 64Mi
        limits:
          memory: 128Mi

    # ---- Rails-App-Server -----------------------------------
    railsserver:
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 1Gi

    # ---- Scheduler (Background Jobs) ------------------------
    scheduler:
      # Läuft (wie railsserver) die volle Rails-App dauerhaft; bei Backlog-
      # Verarbeitung (z.B. viele SearchIndexJobs nach einem Neustart) reichten
      # 512Mi nicht aus → OOMKilled. Limit an railsserver angeglichen.
      resources:
        requests:
          cpu: 50m
          memory: 256Mi
        limits:
          memory: 1Gi

    # ---- Websocket-Server -----------------------------------
    websocket:
      resources:
        requests:
          cpu: 50m
          memory: 256Mi
        limits:
          memory: 512Mi

    # ---- Init-Container ------------------------------------
    initContainers:
      zammad:
        resources:
          requests:
            cpu: 50m
            memory: 256Mi
          limits:
            memory: 512Mi
      postgresql:
        # postgresql-init/-post führen die volle Rails-App aus (DB-Migrationen,
        # Locale-Sync) — vergleichbare Last wie railsserver, daher gleiches Limit.
        resources:
          requests:
            cpu: 50m
            memory: 256Mi
          limits:
            memory: 1Gi

  # ---- PostgreSQL Sub-Chart (Bitnami) -----------------------
  # War auf "nas" (NFS), bis das UGREEN-NAS all_squash statt no_root_squash
  # erzwang und der Bitnami-Entrypoint (chmod auf das Datenverzeichnis)
  # dauerhaft mit "Operation not permitted" scheiterte (docs/16-nas-storage.md).
  # DB-Datenverzeichnis ist reiner State → lokaler Storage auf der
  # Homeserver-SSD statt NAS.
  postgresql:
    enabled: true
    auth:
      username: zammad
      database: zammad_production
      # Passwort aus existierendem SealedSecret
      existingSecret: zammad-secrets
      secretKeys:
        userPasswordKey: postgresql-password
    # local-path legt das Volume-Verzeichnis als root:root (0777) an — fsGroup
    # (siehe primary.podSecurityContext, Chart-Default) setzt nur die Gruppe,
    # nicht den Owner. Ohne volumePermissions scheitert der interne
    # chmod-Fixup des Postgres-Containers (non-root, UID 1001) daher genau
    # wie vorher auf NFS. Bitnami-Chart-eigener Fix: Init-Container, der
    # einmalig als root vorchownt.
    volumePermissions:
      enabled: true
    primary:
      persistence:
        enabled: true
        storageClass: local-path
        size: 50Gi
      # local-path bindet den Pod an den Node, auf dem die PVC zuerst
      # provisioniert wird — fest auf homeserver (aktuell ohnehin der
      # einzige Ready-Node).
      nodeSelector:
        kubernetes.io/hostname: homeserver
      affinity: {}
      resources:
        requests:
          cpu: 50m
          memory: 256Mi
        limits:
          memory: 512Mi

  # ---- Redis Sub-Chart (CloudPirates OCI) -------------------
  # Cache/Websocket-Pub-Sub, ebenfalls auf die NAS ausgelagert, damit kein
  # Persistenzspeicher auf der Homeserver-SSD landet.
  redis:
    auth:
      # Passwort aus existierendem SealedSecret (zammad-secrets[redis-password]).
      # Passwortloses Redis funktioniert mit dem zammad-Chart nicht zuverlässig,
      # siehe Kommentar bei zammad.secrets.redis weiter oben.
      enabled: true
      existingSecret: zammad-secrets
      existingSecretPasswordKey: redis-password
    # Chart-Default setzt updateStrategy.rollingUpdate.maxUnavailable: 1, aber
    # dieser k3s-Cluster hat das Feature-Gate MaxUnavailableStatefulSet nicht
    # aktiviert — der API-Server verwirft das Feld bei jedem Apply, wodurch
    # ArgoCD die Ressource dauerhaft als OutOfSync anzeigt. Per "null"
    # entfernen (Helm-Konvention zum Löschen verschachtelter Default-Keys).
    updateStrategy:
      rollingUpdate:
        maxUnavailable: null
    persistence:
      storageClass: nas
      size: 8Gi
    affinity: {}
