# k3s-Referenz

Dieses Dokument behandelt k3s-Konfiguration, häufige Operationen und Wartung.

---

## Cluster-Topologie

Der k3s-Cluster besteht aus drei Nodes:

| Node       | IP             | Rolle                                 | Ansible-Gruppe |
|------------|----------------|---------------------------------------|----------------|
| homeserver | 192.168.178.94 | Control-Plane + Worker (`k3s server`) | `homeservers`  |
| worker-0   | 192.168.178.95 | Worker (`k3s agent`)                  | `k3s_agents`   |
| worker-1   | 192.168.178.96 | Worker (`k3s agent`)                  | `k3s_agents`   |

worker-0 und worker-1 sind identisch aufgebaute, reine k3s-Compute-Nodes
(kein Docker-Compose-Dienst läuft mehr fest verdrahtet auf einem bestimmten
Host). Der Kubernetes-Scheduler verteilt Pods frei auf alle drei Nodes.
Persistenter Storage liegt zentral auf dem UGREEN NAS (`nas`-StorageClass,
NFS) — jeder Node kann mounten, siehe [docs/16-nas-storage.md](16-nas-storage.md).

### Cluster-Provisionierung

```bash
# Schritt 1: Control-Plane + alle Dienste auf homeserver
make install

# Schritt 2: worker-0 als Worker-Node beitreten lassen
make worker-0

# Schritt 3: worker-1 als Worker-Node beitreten lassen
make worker-1

# Nur den k3s-Agent auf worker-0 neu deployen (z.B. nach Token-Rotation)
make k3s-agent
```

**Bekannter Flake: "D-Bus connection terminated while waiting for jobs"** —
`get-k3s.sh` ruft intern `systemctl enable --now` auf; ist dbus-daemon zu
diesem Zeitpunkt mit paralleler apt/snapd/PackageKit-Aktivität beschäftigt,
bricht die D-Bus-Verbindung ab und der Installer-Task schlägt mit `rc=1` fehl,
obwohl der Service danach meist trotzdem sauber hochkommt. Die Install-Tasks
in `roles/k3s/tasks/main.yml` und `roles/k3s_agent/tasks/main.yml`
wiederholen den (idempotenten) Installer deshalb bis zu 3× mit 10s Abstand.
Bricht der Task trotzdem endgültig ab: `make worker-0` (bzw. `make install`)
einfach erneut laufen lassen.

### Node-Status prüfen

```bash
kubectl get nodes -o wide
# Erwartet:
# NAME          STATUS   ROLES                  AGE   VERSION        INTERNAL-IP
# homeserver    Ready    control-plane,master   Xm    v1.29.x+k3s1  192.168.178.94
# worker-0   Ready    <none>                 Xm    v1.29.x+k3s1  192.168.178.95
# worker-1   Ready    <none>                 Xm    v1.29.x+k3s1  192.168.178.96
```

### Worker-Node verwalten

```bash
# Node cordon (kein neuer Workload wird geplant)
kubectl cordon worker-0

# Node drain (alle Pods evakuieren, z.B. vor Wartung)
kubectl drain worker-0 --ignore-daemonsets --delete-emptydir-data

# Node wieder in Betrieb nehmen
kubectl uncordon worker-0

# Labels anzeigen
kubectl get node worker-0 --show-labels

# Node-Label setzen (z.B. für NodeSelector in Pods)
kubectl label node worker-0 node-role.kubernetes.io/worker=worker
```

### Worker-Node entfernen (permanent)

```bash
# 1. Node im Cluster löschen
kubectl delete node worker-0

# 2. k3s-agent auf worker-0 deinstallieren (via SSH)
ssh ubuntu@192.168.178.95 'sudo /usr/local/bin/k3s-agent-uninstall.sh'
```

---

## Konfigurationsdatei

Die k3s-Server-Konfiguration liegt auf dem Server unter `/etc/rancher/k3s/config.yaml`.

**Pfad:** `/etc/rancher/k3s/config.yaml`

```yaml
# k3s server configuration
write-kubeconfig-mode: "0644"
tls-san:
  - "192.168.1.100"    # Server-LAN-IP
  - "homeserver"       # Hostname
  - "127.0.0.1"        # Loopback
cluster-cidr: "10.42.0.0/16"    # Pod-Netz-CIDR
service-cidr: "10.43.0.0/16"    # Service-Netz-CIDR
node-name: "homeserver"
```

Nach Änderung k3s neu starten:

```bash
sudo systemctl restart k3s
```

**Service-Unit:** `/etc/systemd/system/k3s.service`

**Datenverzeichnis:** `/var/lib/rancher/k3s/`

**Kubeconfig:** `/etc/rancher/k3s/k3s.yaml` (root) und `/home/ubuntu/.kube/config` (ubuntu)

---

## kubectl-Cheatsheet

### Cluster-Status

```bash
# Node-Status
kubectl get nodes
kubectl get nodes -o wide              # mit IPs und OS-Info

# Alle Pods aus allen Namespaces
kubectl get pods -A
kubectl get pods -A -o wide            # mit Node und IP

# Alle Resources in einem Namespace
kubectl get all -n argocd

# Cluster-Info
kubectl cluster-info

# Verfügbare API-Resources
kubectl api-resources
```

### Workload-Operationen

```bash
# Resource beschreiben (Events, Config)
kubectl describe pod <pod-name> -n <namespace>
kubectl describe node homeserver

# Logs
kubectl logs <pod-name> -n <namespace>
kubectl logs <pod-name> -n <namespace> --previous    # vorher abgestürzter Container
kubectl logs <pod-name> -n <namespace> -f            # follow
kubectl logs <pod-name> -n <namespace> -c <container> # spezifischer Container

# Kommando im Pod ausführen
kubectl exec -it <pod-name> -n <namespace> -- /bin/sh

# Port-Forward
kubectl port-forward svc/<service-name> 8080:80 -n <namespace>
kubectl port-forward pod/<pod-name> 8080:80 -n <namespace>

# Deployment skalieren
kubectl scale deployment <name> --replicas=2 -n <namespace>

# Deployment neu starten (Rolling-Restart)
kubectl rollout restart deployment/<name> -n <namespace>

# Rollout-Status
kubectl rollout status deployment/<name> -n <namespace>

# Rollout-Historie
kubectl rollout history deployment/<name> -n <namespace>

# Rollback
kubectl rollout undo deployment/<name> -n <namespace>
```

### Resource-Inspektion

```bash
# YAML einer Resource
kubectl get deployment <name> -n <namespace> -o yaml

# Secret-Wert (base64-decoded)
kubectl get secret <name> -n <namespace> -o jsonpath='{.data.<key>}' | base64 -d

# Top Nodes und Pods (braucht metrics-server)
kubectl top nodes
kubectl top pods -A
kubectl top pods -A --sort-by=cpu
kubectl top pods -A --sort-by=memory

# Events (letzte Stunde)
kubectl get events -A --sort-by='.lastTimestamp'
kubectl get events -n <namespace> --sort-by='.lastTimestamp'
```

### Namespace-Operationen

```bash
# Namespaces auflisten
kubectl get namespaces

# Namespace erstellen
kubectl create namespace my-app

# Namespace löschen (löscht alle Resources darin!)
kubectl delete namespace my-app

# Default-Namespace für aktuellen Context setzen
kubectl config set-context --current --namespace=argocd
```

### Apply & Delete

```bash
# Manifest applyen
kubectl apply -f manifest.yaml
kubectl apply -f directory/            # alle YAMLs im Verzeichnis

# Resource löschen
kubectl delete -f manifest.yaml
kubectl delete pod <name> -n <namespace>
kubectl delete pod <name> -n <namespace> --force --grace-period=0  # Force-Kill

# Dry-Run (Vorschau)
kubectl apply -f manifest.yaml --dry-run=client
kubectl apply -f manifest.yaml --dry-run=server
```

---

## Storage — local-path-Provisioner

k3s liefert die `local-path`-StorageClass standardmäßig mit. Sie provisioniert
PersistentVolumes über Host-Path-Verzeichnisse auf der NVMe-SSD.

```bash
# StorageClass prüfen
kubectl get storageclass

# Erwartet:
# NAME                   PROVISIONER             RECLAIMPOLICY   VOLUMEBINDINGMODE
# local-path (default)   rancher.io/local-path   Delete          WaitForFirstConsumer
```

**Daten-Speicherort auf dem Host:** `/var/lib/rancher/k3s/storage/`

**Beispiel-PVC:**

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-data
  namespace: my-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
```

**Wichtige Limitierungen im Multi-Node-Cluster:**

- Nur `ReadWriteOnce` — nicht parallel von mehreren Pods mountbar.
- Daten leben auf dem Node, auf dem der Pod scheduled wurde — **nicht repliziert**.
  Ein Pod mit einem `local-path`-PVC läuft immer auf demselben Node.
- `Delete`-Reclaim-Policy — beim Löschen der PVC werden die PV-Daten ebenfalls gelöscht.
- `WaitForFirstConsumer` — PV wird erst angelegt, wenn ein Pod gescheduled wird.
- Für Workloads die auf einem bestimmten Node laufen müssen (wegen PVC-Affinität),
  `nodeSelector` oder `nodeName` im Pod/Deployment setzen.

Für größere/geteilte Datenmengen gibt es zusätzlich die NFS-backed
`nas`-StorageClass (UGREEN NAS) — kein `WaitForFirstConsumer`/Node-Pin
nötig, jeder Node kann mounten. Details: [docs/16-nas-storage.md](16-nas-storage.md).

---

## Networking — Flannel VXLAN

k3s nutzt Flannel im VXLAN-Mode für Pod-zu-Pod-Networking.

```bash
# Flannel-Pods
kubectl get pods -n kube-system -l app=flannel

# CNI-Konfiguration
cat /var/lib/rancher/k3s/agent/etc/cni/net.d/10-flannel.conflist

# Pod-Netz-CIDR
kubectl get nodes -o jsonpath='{.items[*].spec.podCIDR}'
```

**Netzbereiche:**

| Bereich         | Zweck                |
|-----------------|----------------------|
| 10.42.0.0/16    | Pod-IPs (Flannel)    |
| 10.43.0.0/16    | Service-ClusterIPs   |
| 10.43.0.10      | CoreDNS-ClusterIP    |

---

## Ingress — Traefik

k3s deployt Traefik v2 als Default-Ingress-Controller und bindet die Host-Ports 80 und 443.

```bash
# Traefik-Pods
kubectl get pods -n kube-system -l app.kubernetes.io/name=traefik

# Traefik-Service (zeigt NodePort-Mapping)
kubectl get svc -n kube-system traefik

# Traefik-Dashboard (aus Sicherheitsgründen standardmäßig deaktiviert)
kubectl port-forward -n kube-system svc/traefik 9000:9000
# Dann http://localhost:9000/dashboard/ öffnen
```

**Standard-Kubernetes-Ingress (Beispiel):**

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-app
  namespace: my-app
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: my-app.homeserver
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-app-svc
                port:
                  number: 80
```

**Traefik-IngressRoute (native CRD):**

```yaml
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: my-app
  namespace: my-app
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`my-app.homeserver`)
      kind: Rule
      services:
        - name: my-app-svc
          port: 80
```

---

## Helm-Integration

Helm wird vom Ansible-Playbook unter `/usr/local/bin/helm` installiert.

```bash
# Helm verifizieren
helm version

# Installierte Releases auflisten
helm list -A

# Release-Values anzeigen
helm get values argocd -n argocd

# Release upgraden
helm upgrade argocd argo/argo-cd -n argocd -f /tmp/my-values.yaml

# Release deinstallieren
helm uninstall argocd -n argocd

# Charts suchen
helm search repo traefik
helm search hub prometheus

# Repo hinzufügen
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update
```

---

## k3s upgraden

Zum Upgrade `k3s_version` in `ansible/group_vars/all.yml` setzen und das Playbook neu laufen lassen:

```bash
# In ansible/group_vars/all.yml
k3s_version: v1.30.1+k3s1

# Nur die k3s-Rolle ausführen
make k3s
# oder:
ansible-playbook -i ansible/inventory/hosts.yml ansible/site.yml --tags k3s --ask-vault-pass
```

Alternativ manuell auf dem Server:

```bash
# Aktuelle Version
k3s --version

# Upgrade via Install-Skript (idempotent, upgraded bei abweichender Version)
curl -sfL https://get.k3s.io | INSTALL_K3S_VERSION=v1.30.1+k3s1 sh -s - server

# Verifizieren
kubectl get nodes
k3s --version
```

**Vor dem Upgrade:**

- [k3s-Releases](https://github.com/k3s-io/k3s/releases) auf Breaking Changes prüfen.
- [Kubernetes-Changelog](https://kubernetes.io/releases/) auf API-Deprecations prüfen.
- `/etc/rancher/k3s/` und `/var/lib/rancher/k3s/` sichern.

---

## k3s-Service-Management

```bash
# Status
sudo systemctl status k3s

# Start/Stop/Restart
sudo systemctl start k3s
sudo systemctl stop k3s
sudo systemctl restart k3s

# k3s-Logs
sudo journalctl -u k3s -f
sudo journalctl -u k3s --since "10 minutes ago"

# k3s-Kernel-Anforderungen prüfen
sudo k3s check-config
```

---

## Backup & Restore

k3s nutzt SQLite als Datastore (auf dem Control-Plane-Node homeserver).
Im Multi-Node-Betrieb läuft die SQLite-DB weiterhin nur auf homeserver.

```bash
# SQLite-DB sichern
sudo cp /var/lib/rancher/k3s/server/db/state.db ~/k3s-backup-$(date +%Y%m%d).db

# Komplettes k3s-Config + Certs sichern
sudo tar -czf ~/k3s-backup-$(date +%Y%m%d).tar.gz \
  /etc/rancher/k3s/ \
  /var/lib/rancher/k3s/server/

# Alle YAML-Manifests exportieren (GitOps-Alternative)
kubectl get all -A -o yaml > ~/k8s-resources-backup-$(date +%Y%m%d).yaml
```

Für kritische Daten ist GitOps der bevorzugte Weg — alle Application-Definitionen
liegen im Git-Repo, ein erneuter Playbook-Run + ArgoCD-Sync stellt den Cluster
in den gewünschten Zustand zurück.
